/// Index file - the entry point of the SPA and the file which renders the SPA routes
pub const INDEX_FILE: &str = "/index.html";

/// File which tells the crawlers what they are allowed to crawl
pub const ROBOTS_FILE: &str = "/robots.txt";
